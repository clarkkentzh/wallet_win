{
  "name": "windows_wallet",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "main": "main.js",
  "dependencies": {
    "antd": "^3.24.2",
    "bignumber.js": "^9.0.0",
    "react": "^16.11.0",
    "react-csv": "^1.1.2",
    "react-dom": "^16.11.0",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron-start": "electron .",
    "package": "electron-packager ./ windows_wallet --platform=mas --arch=x64 --out=./dist --overwrite --electron-version 6.0.10",
    "package_win": "electron-packager ./ windows_wallet --platform=all --arch=x64 --out=./dist --overwrite --electron-version 6.0.10"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-packager": "^14.0.6"
  }
}
